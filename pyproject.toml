[project]
name = "package-r-docs"
version = "0.0.0"
description = "Documentation environment for packageR"
requires-python = ">=3.10"
dependencies = [
  "mike==2.2.0",
  "mkdocs-material==9.7.0",
]

[tool.uv]
package = false
cache-dir = ".uv-cache"
